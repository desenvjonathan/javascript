// criar aplicativo de frases motivacionais

// declaration - declaração da função (Passo um)
// function statement
/* a função fica guardada para ser usada futuramente, ou seja,
o JavaScript lê o código verticalmente e o atalho, nesse caso (createPhrases)
fica salvo e quando pedimos para rodar, ele faz a requisição dessa function */
function createPhrases() {
  console.log('Estudar é muito bom')
  console.log('Paciência e persistência')
  console.log('Revisão é mãe do aprendizado')
}

//executar a função
// rodar, chamar, invocar
// execute, run, call, invoke
createPhrases() // apenas isso é o suficiente para rodar
// caso a gente queira repetir o código várias vezes, basta repetir essa pequena linha de código
createPhrases()
createPhrases()

console.log ('fim do programa') // só para mostrar na execução, que acabou


